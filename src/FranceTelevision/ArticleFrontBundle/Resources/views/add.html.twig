{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form, {'action': path('front_post_form_article')}) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer">
        <a href={{ path('front_index') }}>Retour à la liste</ >
    {{ form_end(form) }}
    
{% endblock %}
